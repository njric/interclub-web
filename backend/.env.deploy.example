# Deployment configuration variables
# Copy this file to .env.deploy and customize for your environment

# Domain configuration
DOMAIN=votre-domaine.com
DOMAIN_WWW=www.votre-domaine.com

# Backend configuration
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
BACKEND_WORKERS=2

# SSL certificates (optional - auto-generated from DOMAIN if not specified)
# Default: /etc/letsencrypt/live/${DOMAIN}/fullchain.pem
# Default: /etc/letsencrypt/live/${DOMAIN}/privkey.pem
# Uncomment to override:
# SSL_CERT_PATH=/custom/path/to/fullchain.pem
# SSL_KEY_PATH=/custom/path/to/privkey.pem
